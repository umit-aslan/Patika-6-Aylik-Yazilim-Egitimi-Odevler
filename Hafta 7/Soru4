#include <stdio.h>

int main() {
    int decimalNumber;

    printf("Bir ondalık sayı girin: ");
    scanf("%d", &decimalNumber);

    printf("Onaltılık (hexadecimal) biçimde: %X\n", decimalNumber);

    return 0;
}
